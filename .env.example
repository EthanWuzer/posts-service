# 'mongo' is the docker-compose service name — do NOT use localhost here.
# The database name at the end (/posts) is required; without it Motor/PyMongo
# defaults to the 'test' database.
MONGO_URI=mongodb://mongo:27017/posts
UPLOAD_DIR=/app/uploads
